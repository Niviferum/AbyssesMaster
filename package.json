{
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "live-server public --port=3000",
    "serve": "npm run build && npm run start"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.32",
    "typescript": "^5.8.3"
  }
}
