{
  "scripts": {
    "build": "tsc && cp -r assets dist/ && cp index.html dist/",
    "dev": "tsc --watch",
    "start": "live-server public --port=3000",
    "serve": "npm run build && npm run start"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "typescript": "^5.8.3"
  }
}
